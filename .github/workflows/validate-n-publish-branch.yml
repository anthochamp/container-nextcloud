name: Validate & publish (branch)

on:
  push:
    branches: [ main ]

jobs:
  validate:
    uses: anthochamp/dev-toolbox/.github/workflows/container-validate.yml@8581130ad2a7165cbf4ba9610cf9d905b292de56

  get-version:
    needs: validate
    uses: anthochamp/dev-toolbox/.github/workflows/util-get-latest-git-version-tag.yml@8581130ad2a7165cbf4ba9610cf9d905b292de56

  pre-publish:
    needs: validate
    uses: ./.github/workflows/nextcloud-pre-publish.yml
    with:
      dockerfile-dir: src

  publish:
    needs: [validate, get-version, pre-publish]
    permissions:
      packages: write
      id-token: write # used for sigstore/fulcio identity challenge
    uses: anthochamp/dev-toolbox/.github/workflows/container-publish.yml@8581130ad2a7165cbf4ba9610cf9d905b292de56
    with:
      dockerfile-dir: src
      dhub-readme-file: ./CONTAINER.md
      version: ${{ needs.get-version.outputs.version }}
      alt-version: ${{ needs.pre-publish.outputs.nextcloud-version }}
      alt-version-prefix: nextcloud
      container-image-name: ${{ needs.pre-publish.outputs.container-image-name }}
      # libsmbclient fails to compile on linux/arm/v7
      build-platforms: linux/amd64,linux/arm64
    secrets:
      dhub-username: ${{ secrets.DHUB_USERNAME }}
      dhub-password: ${{ secrets.DHUB_PASSWORD }}
      ghcr-username: ${{ github.repository_owner }}
      ghcr-password: ${{ secrets.GITHUB_TOKEN }}
